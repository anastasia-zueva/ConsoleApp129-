name: CI

on:
push:
branches:
- main
- master
pull_request:
branches:
- main
- master

jobs:
build:
runs-on: windows-latest

```
steps:
  - uses: actions/checkout@v4

  - uses: microsoft/setup-msbuild@v2

  - uses: NuGet/setup-nuget@v2

  - name: Restore
    run: nuget restore ConsoleApp129.sln

  - name: Build
    run: msbuild ConsoleApp129.sln /p:Configuration=Release

  - name: Test
    run: echo "Tests step"
```
